# Railway config — Stars Market Telegram Bot

[build]
builder = "nixpacks"
buildCommand = "bash build-railway.sh"

[deploy]
startCommand = "bash start.sh"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
